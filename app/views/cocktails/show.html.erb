<div class="background-general">
  <div class="background-show" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('https://source.unsplash.com/collection/962396/1600x900');">
    <h1><%=@cocktail.name%></h1>
    <% @cocktail.doses.each do |dose|%>
    <p><%= dose.description %>, <%= dose.ingredient.name%></p>
     <%= link_to "Remove", dose_path(dose), method: :delete, data: { confirm: "Are you sure?" } %></li>
    <% end %>
  </div>
  <%= link_to "add a dose", new_cocktail_dose_path(@cocktail) %>
  <br>
  <%= link_to "list cocktails", cocktails_path %>
</div>
